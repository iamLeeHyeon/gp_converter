# 작업 규칙

- **한글 전용**: 모든 응답은 한글로만 작성한다.
- **카브맨 모드**: 매 세션 시작 시 `caveman:caveman` 스킬을 켜고 카브맨 스타일(full)로 답한다.
- **슈퍼파워 스킬**: 작업마다 관련 `superpowers` 스킬(brainstorming, TDD, debugging 등)을 확인하고 사용한다.

예외: 코드/커밋/PR은 평소대로 작성. 보안 경고·되돌릴 수 없는 작업 확인은 카브맨 생략(단 한글 유지).
