# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Env / secrets
.env

# 외부 ML 레포 (배포 시 별도 관리)
guitar-tab-omr/

# superpowers scratch (subagent-driven-development progress ledger)
.superpowers/

# 세션 핸드오프 문서 (커밋 대상 아님, 로컬 인수인계용)
handoff/

# Working dirs / outputs
/tmp/
jobs/
*.gp5
*.musicxml
# 단, 테스트 픽스처(합성 악보)는 추적
!tests/fixtures/*.musicxml
.agents/
.claude/skills/
skills-lock.json

# SQLite DB
*.db

# Frontend build output
static/assets/

# impeccable live-mode 세션 상태(임시, 커밋 대상 아님) — config.json은 추적함
.impeccable/live/sessions/
