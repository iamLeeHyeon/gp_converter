[project]
name = "gp-converter"
version = "0.1.0"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: 실제 외부 도구(Audiveris)를 사용하는 테스트",
]
addopts = "-m 'not integration'"
pythonpath = ["."]
